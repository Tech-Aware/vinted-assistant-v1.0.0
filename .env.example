# =============================================================================
# Vinted Assistant - Configuration Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# NEVER commit your .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Google Gemini API (REQUIRED)
# -----------------------------------------------------------------------------
# Get your API key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Gemini model to use (default: gemini-3-pro-preview)
# Available models: gemini-3-pro-preview, gemini-2.5-flash, gemini-2.0-flash
GEMINI_MODEL=gemini-3-pro-preview

# -----------------------------------------------------------------------------
# Google Cloud Vision OCR (OPTIONAL)
# -----------------------------------------------------------------------------
# Path to your Google Cloud service account JSON credentials file
# Get credentials at: https://console.cloud.google.com/apis/credentials
# Required only if you want to use OCR features for label scanning
GOOGLE_APPLICATION_CREDENTIALS=./credentials/gcp-vision.json

# -----------------------------------------------------------------------------
# Logging (OPTIONAL)
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Use INFO or WARNING in production to avoid exposing sensitive data in logs
LOG_LEVEL=INFO
